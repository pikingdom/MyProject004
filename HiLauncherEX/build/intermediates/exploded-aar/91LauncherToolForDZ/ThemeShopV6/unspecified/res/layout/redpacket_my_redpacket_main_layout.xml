<?xml version="1.0" encoding="utf-8"?>
<!-- 我的红包主控制红包页面 -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#ffeef2f6">
    <!-- 可用和不可用红包界面 -->
	<LinearLayout 
	    android:id="@+id/my_readpacket_main_layout_content_layout"
	    android:layout_width="match_parent"
    	android:layout_height="match_parent"
    	android:layout_above="@+id/my_readpacket_main_layout_history_btn"
    	android:orientation="vertical"/>

    <LinearLayout
            android:id="@+id/my_readpacket_main_layout_history_btn"
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:layout_alignParentBottom="true"
            android:background="@color/white"
            android:gravity="center" >

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:singleLine="true"
                android:text="@string/redpacket_my_redpacket_title_tab_history"
                android:textColor="#ff999999"
                android:textSize="13sp" />
        </LinearLayout>
</RelativeLayout>